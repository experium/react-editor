import React from 'react';
import ReactDOM from 'react-dom';
import { FormBuilder } from '../src/index';

ReactDOM.render(
    <FormBuilder placeholder='Тестовый выбор' />, document.getElementById('root'));
